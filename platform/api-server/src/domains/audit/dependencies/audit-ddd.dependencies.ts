/**
 * Audit DDD Dependencies - Composition root
 *
 * Generated by Dependencies Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: audit
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * Audit Domain Module - Composition root
 *
 * DDD: Composition root for audit domain. Exposes repos (ports) and use cases
 * grouped by aggregate. No orgId at build time — use execution context at call time.
 */

import {
  AssurancePackRepositoryAdapter,
  CaseAccessRepositoryAdapter,
  PromotionRepositoryAdapter,
  RetentionRepositoryAdapter,
} from "@aegira/adapters/audit";
import { getIdGeneratorService } from "@aegira/adapters";
import type { AdapterDynamoDBClient } from "@aegira/adapters";
import { executionContextService } from "../../../lib/execution-context.service.js";
// Use-case constructors (concrete)
import {
  ExecuteExportAssurancePack,
  ExecuteGetRetentionPolicy,
  ExecuteListCaseAccessLog,
  ExecuteListPromotionAudit,
  ExecuteUpdateRetentionPolicy,
} from "@aegira/services/audit/usecases";
// Ports (interfaces)
import type {
  AssurancePackRepository,
  CaseAccessRepository,
  PromotionRepository,
  RetentionRepository,
} from "@aegira/services/audit/ports";

export interface AuditDomainModule {
  repos: {
    assurancePacks: AssurancePackRepository;
    caseAccesses: CaseAccessRepository;
    promotions: PromotionRepository;
    retentions: RetentionRepository;
  };
  useCases: {
    assurancePacks: {
      list: ExecuteExportAssurancePack;
    };
    caseAccesses: {
      list: ExecuteListCaseAccessLog;
    };
    promotions: {
      list: ExecuteListPromotionAudit;
    };
    retentions: {
      get: ExecuteGetRetentionPolicy;
      update: ExecuteUpdateRetentionPolicy;
    };
  };
}

export function buildAuditDomainModule(
  dynamoClient: AdapterDynamoDBClient,
): AuditDomainModule {
  // Adapters implementing ports (adapters encapsulate Ddb)
  const repos = (() => {
    const obj: {
      assurancePacks?: AssurancePackRepository;
      caseAccesses?: CaseAccessRepository;
      promotions?: PromotionRepository;
      retentions?: RetentionRepository;
    } = {};

    obj.assurancePacks = new AssurancePackRepositoryAdapter(dynamoClient);
    obj.caseAccesses = new CaseAccessRepositoryAdapter(dynamoClient);
    obj.promotions = new PromotionRepositoryAdapter(dynamoClient);
    obj.retentions = new RetentionRepositoryAdapter(dynamoClient);
    return {
      assurancePacks: obj.assurancePacks!,
      caseAccesses: obj.caseAccesses!,
      promotions: obj.promotions!,
      retentions: obj.retentions!,
    };
  })();

  const executionContext = executionContextService;
  const idGenerator = getIdGeneratorService();

  // Use cases depend only on ports (repos.*)
  const useCases = {
    assurancePacks: {
      list: new ExecuteExportAssurancePack(executionContext, idGenerator, repos.assurancePacks),
    },
    caseAccesses: {
      list: new ExecuteListCaseAccessLog(executionContext, idGenerator, repos.caseAccesses),
    },
    promotions: {
      list: new ExecuteListPromotionAudit(executionContext, idGenerator, repos.promotions),
    },
    retentions: {
      get: new ExecuteGetRetentionPolicy(executionContext, idGenerator, repos.retentions),
      update: new ExecuteUpdateRetentionPolicy(executionContext, idGenerator, repos.retentions),
    },
  };
  return { repos, useCases: useCases as AuditDomainModule["useCases"] };
}
