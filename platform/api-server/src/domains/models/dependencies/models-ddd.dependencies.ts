/**
 * Models DDD Dependencies - Composition root
 *
 * Generated by Dependencies Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: models
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * Models Domain Module - Composition root
 *
 * DDD: Composition root for models domain. Exposes repos (ports) and use cases
 * grouped by aggregate. No orgId at build time — use execution context at call time.
 */

import {
  ApproveRepositoryAdapter,
  PromotionRepositoryAdapter,
  RejectRepositoryAdapter,
  RollbackRepositoryAdapter,
  VModelRepositoryAdapter,
} from "@aegira/adapters/models";
import { getIdGeneratorService } from "@aegira/adapters";
import type { AdapterDynamoDBClient } from "@aegira/adapters";
import { executionContextService } from "../../../lib/execution-context.service.js";
// Use-case constructors (concrete)
import {
  ExecuteApprovePromotion,
  ExecuteGetFraudModel,
  ExecuteListFraudModels,
  ExecuteListPromotionDecisions,
  ExecuteProposePromotion,
  ExecuteRejectPromotion,
  ExecuteRollbackPromotion,
} from "@aegira/services/models/usecases";
// Ports (interfaces)
import type {
  ApproveRepository,
  PromotionRepository,
  RejectRepository,
  RollbackRepository,
  VModelRepository,
} from "@aegira/services/models/ports";

export interface ModelsDomainModule {
  repos: {
    approves: ApproveRepository;
    promotions: PromotionRepository;
    rejects: RejectRepository;
    rollbacks: RollbackRepository;
    vModels: VModelRepository;
  };
  useCases: {
    approves: {
      create: ExecuteApprovePromotion;
    };
    promotions: {
      get: ExecuteProposePromotion;
      list: ExecuteListPromotionDecisions;
    };
    rejects: {
      create: ExecuteRejectPromotion;
    };
    rollbacks: {
      get: ExecuteRollbackPromotion;
    };
    vModels: {
      get: ExecuteGetFraudModel;
      list: ExecuteListFraudModels;
    };
  };
}

export function buildModelsDomainModule(
  dynamoClient: AdapterDynamoDBClient,
): ModelsDomainModule {
  // Adapters implementing ports (adapters encapsulate Ddb)
  const repos = (() => {
    const obj: {
      approves?: ApproveRepository;
      promotions?: PromotionRepository;
      rejects?: RejectRepository;
      rollbacks?: RollbackRepository;
      vModels?: VModelRepository;
    } = {};

    obj.approves = new ApproveRepositoryAdapter(dynamoClient);
    obj.promotions = new PromotionRepositoryAdapter(dynamoClient);
    obj.rejects = new RejectRepositoryAdapter(dynamoClient);
    obj.rollbacks = new RollbackRepositoryAdapter(dynamoClient);
    obj.vModels = new VModelRepositoryAdapter(dynamoClient);
    return {
      approves: obj.approves!,
      promotions: obj.promotions!,
      rejects: obj.rejects!,
      rollbacks: obj.rollbacks!,
      vModels: obj.vModels!,
    };
  })();

  const executionContext = executionContextService;
  const idGenerator = getIdGeneratorService();

  // Use cases depend only on ports (repos.*)
  const useCases = {
    approves: {
      create: new ExecuteApprovePromotion(executionContext, idGenerator, repos.approves),
    },
    promotions: {
      get: new ExecuteProposePromotion(executionContext, idGenerator, repos.promotions),
      list: new ExecuteListPromotionDecisions(executionContext, idGenerator, repos.promotions),
    },
    rejects: {
      create: new ExecuteRejectPromotion(executionContext, idGenerator, repos.rejects),
    },
    rollbacks: {
      get: new ExecuteRollbackPromotion(executionContext, idGenerator, repos.rollbacks),
    },
    vModels: {
      get: new ExecuteGetFraudModel(executionContext, idGenerator, repos.vModels),
      list: new ExecuteListFraudModels(executionContext, idGenerator, repos.vModels),
    },
  };
  return { repos, useCases: useCases as ModelsDomainModule["useCases"] };
}
