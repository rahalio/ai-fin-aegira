/**
 * ExecuteScoreTransaction - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: scoring
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteScoreTransaction Use Case
 *
 * DDD: Application use case for get Score.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { ScoreTransactionInput, ScoreTransactionOutput } from "../dto/score.dto";
import type { ExecutionContextService, IdGeneratorService } from "@aegira/services/_shared/index.js";
import type { ScoreRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteScoreTransaction {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly score: ScoreRepository
  ) {}

  async execute(input: ScoreTransactionInput): Promise<ScoreTransactionOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.scrId();

    // All field names extracted from OpenAPI spec: amount, deviceId, features, merchantId, transactionRef
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).deviceId) {
      throw new ValidationError("deviceId is required");
    }
    const deviceId = (input as any).deviceId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.score.scoreTransaction({ deviceId: deviceId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${deviceId}`);
    }

    return entity as ScoreTransactionOutput;
  }
}
