/**
 * ExecuteAcknowledgeLatencyBreach - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: scoring
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteAcknowledgeLatencyBreach Use Case
 *
 * DDD: Application use case for get Acknowledge.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { AcknowledgeLatencyBreachInput, AcknowledgeLatencyBreachOutput } from "../dto/acknowledge.dto";
import type { ExecutionContextService, IdGeneratorService } from "@aegira/services/_shared/index.js";
import type { AcknowledgeRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteAcknowledgeLatencyBreach {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly acknowledge: AcknowledgeRepository
  ) {}

  async execute(input: AcknowledgeLatencyBreachInput): Promise<AcknowledgeLatencyBreachOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.scrId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).breachId) {
      throw new ValidationError("breachId is required");
    }
    const breachId = (input as any).breachId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.acknowledge.acknowledgeLatencyBreach({ breachId: breachId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${breachId}`);
    }

    return entity as AcknowledgeLatencyBreachOutput;
  }
}
