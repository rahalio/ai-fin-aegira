/**
 * ExecuteProposePromotion - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: models
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteProposePromotion Use Case
 *
 * DDD: Application use case for get Promotion.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { ProposePromotionInput, ProposePromotionOutput } from "../dto/promotion.dto";
import type { ExecutionContextService, IdGeneratorService } from "@aegira/services/_shared/index.js";
import type { PromotionRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteProposePromotion {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly promotion: PromotionRepository
  ) {}

  async execute(input: ProposePromotionInput): Promise<ProposePromotionOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.mdlId();

    // All field names extracted from OpenAPI spec: challengerModelId, championModelId, experimentId
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).challengerModelId) {
      throw new ValidationError("challengerModelId is required");
    }
    const challengermodelId = (input as any).challengerModelId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.promotion.proposePromotion({ challengerModelId: challengermodelId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${challengermodelId}`);
    }

    return entity as ProposePromotionOutput;
  }
}
