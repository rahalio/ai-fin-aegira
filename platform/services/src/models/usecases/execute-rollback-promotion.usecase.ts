/**
 * ExecuteRollbackPromotion - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: models
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteRollbackPromotion Use Case
 *
 * DDD: Application use case for get Rollback.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { RollbackPromotionInput, RollbackPromotionOutput } from "../dto/rollback.dto";
import type { ExecutionContextService, IdGeneratorService } from "@aegira/services/_shared/index.js";
import type { RollbackRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteRollbackPromotion {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly rollback: RollbackRepository
  ) {}

  async execute(input: RollbackPromotionInput): Promise<RollbackPromotionOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.mdlId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).promotionId) {
      throw new ValidationError("promotionId is required");
    }
    const promotionId = (input as any).promotionId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.rollback.rollbackPromotion({ promotionId: promotionId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${promotionId}`);
    }

    return entity as RollbackPromotionOutput;
  }
}
