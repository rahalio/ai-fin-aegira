/**
 * ExecuteGetPriorityPolicy - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: cases
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteGetPriorityPolicy Use Case
 *
 * DDD: Application use case for get PriorityPolicy.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { GetPriorityPolicyInput, GetPriorityPolicyOutput } from "../dto/priority-policy.dto";
import type { ExecutionContextService, IdGeneratorService } from "@aegira/services/_shared/index.js";
import type { PriorityPolicyRepository } from "../ports";
import { NotFoundError } from "../errors";

export class ExecuteGetPriorityPolicy {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly priorityPolicy: PriorityPolicyRepository
  ) {}

  async execute(input: GetPriorityPolicyInput): Promise<GetPriorityPolicyOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.cseId();

    // All field names extracted from OpenAPI spec: none
    // Load entity via repository (no ID field - uses input directly, pass correlation ID for response meta)
    const entity = await this.priorityPolicy.getPriorityPolicy({ ...input, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found`);
    }

    return entity as GetPriorityPolicyOutput;
  }
}
