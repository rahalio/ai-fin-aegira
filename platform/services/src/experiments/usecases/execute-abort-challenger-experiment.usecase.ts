/**
 * ExecuteAbortChallengerExperiment - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: experiments
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteAbortChallengerExperiment Use Case
 *
 * DDD: Application use case for get Abort.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { AbortChallengerExperimentInput, AbortChallengerExperimentOutput } from "../dto/abort.dto";
import type { ExecutionContextService, IdGeneratorService } from "@aegira/services/_shared/index.js";
import type { AbortRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteAbortChallengerExperiment {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly abort: AbortRepository
  ) {}

  async execute(input: AbortChallengerExperimentInput): Promise<AbortChallengerExperimentOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.expId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).experimentId) {
      throw new ValidationError("experimentId is required");
    }
    const experimentId = (input as any).experimentId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.abort.abortChallengerExperiment({ experimentId: experimentId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${experimentId}`);
    }

    return entity as AbortChallengerExperimentOutput;
  }
}
