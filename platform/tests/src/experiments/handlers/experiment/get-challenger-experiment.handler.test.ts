/**
 * GetChallengerExperiment handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: experiments
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getChallengerExperiment } from "@aegira/api-server/domains/experiments/handlers";
import type { ExperimentsDDDDependencies } from "@aegira/api-server/domains/experiments";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@aegira/tests/shared";
import { EXPERIMENTS_USE_CASES } from "../__factories__/use-cases.js";

describe("GetChallengerExperiment", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: ExperimentsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<ExperimentsDDDDependencies>(
      EXPERIMENTS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = { id: 'mock-id', orgId } as any;
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetChallengerExperiment, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { experimentId: entity.id };

    await getChallengerExperiment(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetChallengerExperiment.execute).toHaveBeenCalledWith(expect.objectContaining({ experimentId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { experimentId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetChallengerExperiment, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getChallengerExperiment(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
