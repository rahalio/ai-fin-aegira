/**
 * AcknowledgeLatencyBreach handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: scoring
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { acknowledgeLatencyBreach } from "@aegira/api-server/domains/scoring/handlers";
import type { ScoringDDDDependencies } from "@aegira/api-server/domains/scoring";
import { createCreateAcknowledgeRequest } from "@aegira/factories/scoring";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@aegira/tests/shared";
import { SCORING_USE_CASES } from "../__factories__/use-cases.js";

describe("AcknowledgeLatencyBreach", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: ScoringDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<ScoringDDDDependencies>(
      SCORING_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 201 with created entity", async () => {
    const input = createCreateAcknowledgeRequest();
    const entity = { id: 'mock-id', orgId } as any;
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    mockRequest.body = input;
    vi.spyOn(mockDeps.executeAcknowledgeLatencyBreach, "execute").mockResolvedValue(useCaseResult);

    await acknowledgeLatencyBreach(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeAcknowledgeLatencyBreach.execute).toHaveBeenCalledWith(expect.anything());
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
