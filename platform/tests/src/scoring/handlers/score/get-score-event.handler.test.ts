/**
 * GetScoreEvent handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: scoring
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getScoreEvent } from "@aegira/api-server/domains/scoring/handlers";
import type { ScoringDDDDependencies } from "@aegira/api-server/domains/scoring";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@aegira/tests/shared";
import { SCORING_USE_CASES } from "../__factories__/use-cases.js";

describe("GetScoreEvent", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: ScoringDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<ScoringDDDDependencies>(
      SCORING_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = { id: 'mock-id', orgId } as any;
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetScoreEvent, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { scoreEventId: entity.id };

    await getScoreEvent(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetScoreEvent.execute).toHaveBeenCalledWith(expect.objectContaining({ scoreEventId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { scoreEventId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetScoreEvent, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getScoreEvent(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
