/**
 * AmlHandoff Entity Factory
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: handoffs
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { faker } from "@faker-js/faker";
import type { AmlHandoffEntity } from "@aegira/core/handoffs/models/index.js";
import { handoffsSchemas } from "@aegira/core/handoffs";
import { ulid } from "ulid";

// Domain prefix from OpenAPI spec info.x-domain
const DOMAIN_PREFIX = "hnd";

export function createAmlHandoff(overrides: Partial<AmlHandoffEntity> = {}) {
  const raw = {
    id: overrides.id ?? `${DOMAIN_PREFIX}_${ulid().toLowerCase()}`,
    createdAt: overrides.createdAt ?? new Date().toISOString(),
    updatedAt: overrides.updatedAt ?? new Date().toISOString(),
    caseId: overrides.caseId ?? `cse_${ulid().toLowerCase()}`,
    handoffId: overrides.handoffId ?? `hnd_${ulid().toLowerCase()}`,
    reasonCode: overrides.reasonCode ?? faker.lorem.word(),
    status: overrides.status ?? "submitted",
    ...overrides,
  };

  return handoffsSchemas.AmlHandoff.parse(raw);
}

export function amlHandoffBatch(n = 3, overrides = {}) {
  return Array.from({ length: n }, () => createAmlHandoff(overrides));
}
