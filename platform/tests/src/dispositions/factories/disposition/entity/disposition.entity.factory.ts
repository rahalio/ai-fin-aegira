/**
 * Disposition Entity Factory
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: dispositions
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { faker } from "@faker-js/faker";
import type { DispositionEntity } from "@aegira/core/dispositions/models/index.js";
import { dispositionsSchemas } from "@aegira/core/dispositions";
import { ulid } from "ulid";

// Domain prefix from OpenAPI spec info.x-domain
const DOMAIN_PREFIX = "dsp";

export function createDisposition(overrides: Partial<DispositionEntity> = {}) {
  const raw = {
    id: overrides.id ?? `${DOMAIN_PREFIX}_${ulid().toLowerCase()}`,
    createdAt: overrides.createdAt ?? new Date().toISOString(),
    updatedAt: overrides.updatedAt ?? new Date().toISOString(),
    caseId: overrides.caseId ?? `cse_${ulid().toLowerCase()}`,
    code: overrides.code ?? "fraud_confirmed",
    dispositionId: overrides.dispositionId ?? `dsp_${ulid().toLowerCase()}`,
    locked: overrides.locked ?? faker.datatype.boolean(),
    ...overrides,
  };

  return dispositionsSchemas.Disposition.parse(raw);
}

export function dispositionBatch(n = 3, overrides = {}) {
  return Array.from({ length: n }, () => createDisposition(overrides));
}
