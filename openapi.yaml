openapi: 3.0.3
info:
  title: Aegira
  version: 0.1.0
  description: |
    Pointer only. Canonical OpenAPI for codegen lives in
    packages/openapi-core/src/{domain}.yaml (one file per domain).
  x-product-slug: ai-fin-aegira
servers:
  - url: https://api.example.com
paths: {}
